/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media4.giphy.com', 'cdn3.sbnation.com', 'thumbs.gfycat.com', "media.tenor.com"], // Reemplaza 'example.com' con los dominios de tus imágenes
    },
}

module.exports = nextConfig
